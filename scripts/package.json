{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Package with scripts using in this repository.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "octokit": "3.2.1"
  },
  "devDependencies": {
    "@types/node": "20.12.10",
    "typescript": "5.4.5"
  }
}
