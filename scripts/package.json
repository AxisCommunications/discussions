{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Package with scripts using in this repository.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "octokit": "5.0.3"
  },
  "devDependencies": {
    "@types/node": "22.17.2",
    "typescript": "5.9.2"
  }
}
